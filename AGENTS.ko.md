# 개발

[English](AGENTS.md)

`split-pane`은 헤드리스 배치 라이브러리다. 라이브러리는 카드 좌표와 선택적 DOM 바인딩을 담당한다. 예제 애플리케이션은 프로젝트, 탭, 스타일, 네이티브 뷰, 셸을 담당한다.

## 구현

- 현재 계약을 충족하는 가장 단순한 구현을 선택한다. 호환 계층, 폴백, 마이그레이션을 추가하지 않고 오래된 경로를 제거한다.
- 책임을 분리한다. 기존 API로 필요한 동작을 구현할 수 없는 경우에만 네이티브 플랫폼 코드를 추가한다.
- 해롭거나 불필요한 변경을 제거한다. 옳지만 무관한 변경은 별도로 유지하고 커밋에 실제 목적을 기록한다.
- 커밋을 재작성하기 전에 동작을 검증한다. 승인 없이 병합하거나 푸시하지 않는다.

## 문서

- 동작을 변경하기 전에 해당 명세를 읽는다. 계약이 바뀌면 명세를 먼저 수정하고 [기능 상태](docs/features.ko.md)에 미구현 부분을 표시한다.
- 주제마다 정본을 하나 둔다. `docs/spec/`는 계약, `docs/features.md`는 구현·검증·배포 상태, `docs/operations/`는 현재 절차, `CHANGELOG.md`는 실제 변경과 검증을 관리한다.
- README는 소개, 최소 시작 절차, 문서 링크로 제한한다. `docs/plans/`에는 승인 대기 제안만 작성하고, 승인 내용을 명세에 반영한 뒤 제안서를 제거한다.
- 영어가 정본이다. 같은 변경에서 대응하는 `.ko.md` 번역을 동일한 정보로 갱신한다.
- 현재 동작과 실제 이유를 서술한다. 개인 선호와 대화 맥락은 Git 외부 메모리에 기록한다.
- 동작 이름을 직접 사용하고 주체와 대상을 명시한다. 원인은 한 문장으로 설명한다. 주석·문서·로그·커밋에 비유, 의인화, 구어체를 사용하지 않는다.
- 라이브러리 주석은 영어, 예제 주석은 한국어로 작성한다. 식별자, 로그, 오류, 테스트 이름은 영어로 작성한다.
- 커밋 메시지에 트레일러, 자동 생성 표시, 세션 링크를 추가하지 않는다.

## 검증

- 모든 변경에 `make docs-check`를 실행하고 문서와 코드를 대조한다. 구조 검사는 내용의 정확성을 검증하지 않는다.
- 라이브러리 변경에 `make verify`를 실행한다. 생성된 `dist/` 변경을 소스와 함께 커밋한다. 마지막 검사는 소스의 빌드 결과와 커밋한 파일이 같아야 통과한다.
- 네이티브·예제 동작은 [예제 검증](docs/operations/examples.ko.md)을 따른다. 두 호스트를 빌드한 뒤 해당 바이너리로 관련 검사를 실행한다.
- 창 검사는 `--observe`로 실행 중인 앱에 연결한다. 앱을 실행하거나 창을 활성화하지 않는다.
- 녹화는 요청한 속도의 제스처 전체를 포함해야 한다. 프레임 누락이나 입력 미완료는 통과가 아니라 실패다.
- 검증한 구현과 플랫폼을 기록한다. 이전 결과로 이후 코드를 검증하거나 테스트 통과를 배포로 기록하지 않는다.
